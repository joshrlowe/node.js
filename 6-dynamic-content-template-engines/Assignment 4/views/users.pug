extends layouts/main-layout.pug
block title
    if pageTitle
        title #{pageTitle}
    else 
        title Users
block body 
    if users.length > 0
        ul 
            each user in users 
                li #{user}
    else 
        h1 No Users