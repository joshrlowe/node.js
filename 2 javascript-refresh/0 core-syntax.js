// Declare and initialize user attributes
const userName = "Josh"; // Name of the user as a string
const userAge = 23; // Age of the user as a number
const userHasHobbies = true; // User's hobbies status as a boolean

// Function to summarize user information
function summarizeUser(userName, userAge, userHasHobby) {
  return (
    "Name is " +
    userName +
    ", age is " +
    userAge +
    " and the user has hobbies: " +
    userHasHobby
  );
}

// Output the summary of the user information
console.log(summarizeUser(name, age, hasHobbies));
